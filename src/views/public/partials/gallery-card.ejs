<div class="gallery_item fade-up">
  <a
    href="<%= item.image %>"
    data-lightbox-image
    <% if (typeof galleryGroup !== "undefined" && galleryGroup) { %>data-lightbox-group="<%= galleryGroup %>"<% } %>
    aria-label="View <%= item.title %>"
  >
    <img
      src="<%= item.thumb || item.image %>"
      alt="<%= item.title %>"
      loading="lazy"
      decoding="async"
    />
  </a>
</div>
